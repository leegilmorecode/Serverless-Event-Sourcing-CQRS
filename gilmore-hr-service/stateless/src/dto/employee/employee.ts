export type EmployeeDetails = {
  firstName: string;
  surname: string;
};

export type Employee = {
  id: string;
  lastUpdated: string;
  firstName: string;
  surname: string;
  amount: number;
  version: number;
};
